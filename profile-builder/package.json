{
  "name": "@opportunity-radar/profile-builder",
  "version": "2.0.0",
  "description": "Candidate Intelligence Profile Builder — converts a parsed resume into a versioned, schema-validated CIP and persists it to the Data Plane.",
  "main": "src/index.js",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check src/config.js && node --check src/gateway-client.js && node --check src/profile-builder.js && node --check src/index.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
