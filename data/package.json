{
  "name": "@opportunity-radar/data",
  "version": "1.0.0",
  "description": "Data Plane infrastructure for Opportunity Radar: PostgreSQL client, Redis client, schema migrations, and health-check utilities.",
  "main": "src/index.js",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "migrate": "node src/migrate.js",
    "health": "node src/health-check.js",
    "test": "node --test test/*.test.js",
    "check": "node --check src/config.js && node --check src/db.js && node --check src/redis.js && node --check src/migrate.js && node --check src/health-check.js && node --check src/index.js && node --check src/canonical-link-resolver.js && node --check src/duplicate-detector.js && node --check src/schemas/candidate-profile-schema.js && node --check src/repositories/candidate-repository.js && node --check src/repositories/search-plan-repository.js && node --check src/repositories/opportunity-repository.js && node --check src/repositories/execution-run-repository.js && node --check src/repositories/dedup-repository.js && node --check src/repositories/event-log-repository.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "fast-fuzzy": "^1.12.0",
    "ioredis": "^5.3.2",
    "pg": "^8.12.0"
  }
}
