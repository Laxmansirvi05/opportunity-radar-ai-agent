{
  "name": "@opportunity-radar/data",
  "version": "1.0.0",
  "description": "Data Plane infrastructure for Opportunity Radar: PostgreSQL client, Redis client, schema migrations, and health-check utilities.",
  "main": "src/index.js",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "migrate": "node src/migrate.js",
    "health": "node src/health-check.js",
    "check": "node --check src/config.js && node --check src/db.js && node --check src/redis.js && node --check src/migrate.js && node --check src/health-check.js && node --check src/index.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "ioredis": "^5.3.2",
    "pg": "^8.12.0"
  }
}
